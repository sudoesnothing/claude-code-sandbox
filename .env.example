# Claude Code Sandbox — environment configuration
# Copy to .env and adjust to your machine's available resources. Never commit .env.

# Memory cap for the container (e.g. 4g, 8g, 16g)
SANDBOX_MEMORY_LIMIT=8g

# Number of vCPUs available to the container
SANDBOX_CPU_LIMIT=4
